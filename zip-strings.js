const strA = 'abc'
const strB = '123'

function zipStrings(strA, strB) {
  // Add you solution here
  const stringC = strA.concat(strB)
  return(stringC.toString())
}

console.log(zipStrings(strA, strB));
